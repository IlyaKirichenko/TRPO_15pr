<Page x:Class="TRPO_KI_15pr_ElectronicShop.Pages.ManageTags"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TRPO_KI_15pr_ElectronicShop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Управление тегами" Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Управление тегами"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 10"/>
        <StackPanel Grid.Row="1" 
                    Margin=" 0 0 0 10">
            <TextBlock Text="Добавить тег" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontSize="16" Text="Название" Margin="5" VerticalAlignment="Center"/>
                <TextBox x:Name="txtName" Grid.Column="1" Margin="5" Background="#F0F0F0"/>
                <Button Grid.Column="2" Content="Добавить" Background="#00C4B4" Width="100" Height="30" Margin="5" Click="Add_Click"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Список тегов" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <ListView x:Name="lvTag" Grid.Row="1" 
                      SelectionChanged="lvTag_SelectionChanged"
                      Background="White" 
                      BorderBrush="#00b4c4" 
                      BorderThickness="1">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Название" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Редактировать" Background="#00C4B4" Width="130" Height="35" Margin="5" Click="Update_Click"/>
                <Button Content="Удалить" Background="#00b4c4" Foreground="White" Width="100" Height="35" Margin="5" Click="Delete_Click"/>
                <Button Content="Назад" Background="#00C4B4" Width="100" Height="35" Margin="5" Click="Back_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
